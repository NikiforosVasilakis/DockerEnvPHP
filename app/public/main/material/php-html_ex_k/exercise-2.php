<?php
// Declare an array with three names
$names = ["George", "Marry", "Nick"];

// Loop through the array and print each name
foreach ($names as $name) {
    // Display each name one by one
    echo $name . "<br>";
}