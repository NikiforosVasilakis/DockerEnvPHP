<?php
// Function that returns the greater of two numbers
function findMax($num1, $num2) {
    // Use a conditional (ternary) operator to return the greater number
    return ($num1 > $num2) ? $num1 : $num2;
}
